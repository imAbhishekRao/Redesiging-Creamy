{{ 'component-cart.css' | asset_url | stylesheet_tag }}
{{ 'component-cart-items.css' | asset_url | stylesheet_tag }}
{{ 'component-totals.css' | asset_url | stylesheet_tag }}
{{ 'component-price.css' | asset_url | stylesheet_tag }}
{{ 'component-discounts.css' | asset_url | stylesheet_tag }}
{{ 'quantity-popover.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }

  /* Modern Cart Design Styles */
  .modern-cart {
    background: #f8f8f8;
    min-height: 100vh;
  }

  .modern-cart__container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem;
  }

  .modern-cart__header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 2rem;
    padding-bottom: 1rem;
    border-bottom: 2px solid #e5e5e5;
  }

  .modern-cart__title {
    font-size: 2.4rem;
    font-weight: 700;
    color: #333;
    margin: 0;
  }

  .modern-cart__close {
    background: none;
    border: none;
    font-size: 2rem;
    cursor: pointer;
    color: #666;
    padding: 0.5rem;
  }

  .modern-cart__items {
    margin-bottom: 2rem;
  }

  .modern-cart-item {
    display: flex;
    align-items: center;
    padding: 1.5rem;
    background: white;
    border-radius: 12px;
    margin-bottom: 1rem;
    box-shadow: 0 2px 8px rgba(0,0,0,0.1);
  }

  .modern-cart-item__image {
    width: 120px;
    height: 120px;
    object-fit: cover;
    border-radius: 8px;
    margin-right: 1.5rem;
  }

  .modern-cart-item__details {
    flex: 1;
  }

  .modern-cart-item__title {
    font-size: 1.6rem;
    font-weight: 600;
    color: #333;
    margin-bottom: 0.5rem;
  }

  .modern-cart-item__variant {
    font-size: 1.4rem;
    color: #666;
    margin-bottom: 1rem;
  }

  .modern-cart-item__quantity {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1rem;
  }

  .modern-cart-item__quantity-controls {
    display: flex;
    align-items: center;
    border: 1px solid #ddd;
    border-radius: 6px;
    overflow: hidden;
  }

  .modern-cart-item__quantity-btn {
    background: #f8f8f8;
    border: none;
    padding: 0.8rem 1.2rem;
    cursor: pointer;
    font-size: 1.4rem;
    color: #333;
  }

  .modern-cart-item__quantity-input {
    border: none;
    text-align: center;
    width: 60px;
    padding: 0.8rem;
    font-size: 1.4rem;
  }

  .modern-cart-item__remove {
    background: none;
    border: none;
    color: #999;
    font-size: 1.4rem;
    cursor: pointer;
    margin-left: 1rem;
  }

  .modern-cart-item__price {
    text-align: right;
    min-width: 120px;
  }

  .modern-cart-item__price-current {
    font-size: 1.8rem;
    font-weight: 700;
    color: #e53e3e;
  }

  .modern-cart-item__price-original {
    font-size: 1.4rem;
    color: #999;
    text-decoration: line-through;
  }

  .modern-cart__gift-bag {
    display: flex;
    align-items: center;
    padding: 1.5rem;
    background: white;
    border-radius: 12px;
    margin-bottom: 2rem;
    box-shadow: 0 2px 8px rgba(0,0,0,0.1);
  }

  .modern-cart__gift-bag-image {
    width: 60px;
    height: 60px;
    object-fit: cover;
    border-radius: 6px;
    margin-right: 1rem;
  }

  .modern-cart__gift-bag-text {
    flex: 1;
    font-size: 1.4rem;
    color: #333;
  }

  .modern-cart__gift-bag-checkbox {
    width: 20px;
    height: 20px;
    accent-color: #8b4513;
  }

  .modern-cart__summary {
    background: white;
    border-radius: 12px;
    padding: 2rem;
    margin-bottom: 2rem;
    box-shadow: 0 2px 8px rgba(0,0,0,0.1);
  }

  .modern-cart__summary-row {
    display: flex;
    justify-content: space-between;
    margin-bottom: 1rem;
    font-size: 1.6rem;
  }

  .modern-cart__summary-total {
    font-weight: 700;
    font-size: 2rem;
    border-top: 2px solid #e5e5e5;
    padding-top: 1rem;
    margin-top: 1rem;
  }

  .modern-cart__checkout-btn {
    width: 100%;
    background: #8b4513;
    color: white;
    border: none;
    padding: 1.5rem;
    border-radius: 8px;
    font-size: 1.6rem;
    font-weight: 600;
    cursor: pointer;
    margin-bottom: 1rem;
  }

  .modern-cart__view-bag {
    text-align: center;
    text-decoration: underline;
    color: #666;
    font-size: 1.4rem;
    cursor: pointer;
  }

  .modern-cart__recommendations {
    margin-top: 3rem;
  }

  .modern-cart__recommendations-title {
    font-size: 2rem;
    font-weight: 700;
    color: #333;
    margin-bottom: 1.5rem;
  }

  .modern-cart__recommendations-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1.5rem;
    margin-bottom: 2rem;
  }

  .modern-cart__recommendation-item {
    background: white;
    border-radius: 12px;
    padding: 1rem;
    box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    text-align: center;
  }

  .modern-cart__recommendation-image {
    width: 100%;
    height: 150px;
    object-fit: cover;
    border-radius: 8px;
    margin-bottom: 1rem;
  }

  .modern-cart__recommendation-title {
    font-size: 1.4rem;
    font-weight: 600;
    color: #333;
    margin-bottom: 0.5rem;
  }

  .modern-cart__recommendation-price {
    font-size: 1.6rem;
    font-weight: 700;
    color: #e53e3e;
    margin-bottom: 1rem;
  }

  .modern-cart__recommendation-heart {
    background: none;
    border: none;
    font-size: 1.8rem;
    color: #999;
    cursor: pointer;
  }

  .modern-cart__policies {
    margin-top: 3rem;
    padding-top: 2rem;
    border-top: 2px solid #e5e5e5;
  }

  .modern-cart__policy-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem 0;
    border-bottom: 1px solid #f0f0f0;
    cursor: pointer;
  }

  .modern-cart__policy-text {
    font-size: 1.4rem;
    color: #333;
  }

  .modern-cart__policy-arrow {
    font-size: 1.2rem;
    color: #999;
  }

  @media (max-width: 768px) {
    .modern-cart__container {
      padding: 1rem;
    }
    
    .modern-cart-item {
      flex-direction: column;
      text-align: center;
    }
    
    .modern-cart-item__image {
      margin-right: 0;
      margin-bottom: 1rem;
    }
    
    .modern-cart-item__price {
      text-align: center;
      margin-top: 1rem;
    }
  }
{%- endstyle -%}

{%- unless settings.cart_type == 'drawer' -%}
  <script src="{{ 'cart.js' | asset_url }}" defer="defer"></script>
{%- endunless -%}

<script src="{{ 'quantity-popover.js' | asset_url }}" defer="defer"></script>

<cart-items class="modern-cart gradient color-{{ section.settings.color_scheme }} isolate{% if cart == empty %} is-empty{% else %} section-{{ section.id }}-padding{% endif %}">
  <div class="modern-cart__container">
    <!-- Header -->
    <div class="modern-cart__header">
      <h1 class="modern-cart__title">MY BAG ({{ cart.item_count }})</h1>
      <button class="modern-cart__close" onclick="window.history.back()">×</button>
    </div>

    {%- if cart == empty -%}
      <div class="modern-cart__empty">
        <h2>Your cart is empty</h2>
        <a href="{{ routes.all_products_collection_url }}" class="modern-cart__checkout-btn">
          Continue Shopping
        </a>
      </div>
    {%- else -%}
      <!-- Cart Items -->
      <div class="modern-cart__items">
        <form action="{{ routes.cart_url }}" method="post" id="cart">
          {%- for item in cart.items -%}
            <div class="modern-cart-item" id="CartItem-{{ item.index | plus: 1 }}">
              <img 
                src="{{ item.image | image_url: width: 300 }}" 
                alt="{{ item.image.alt | escape }}"
                class="modern-cart-item__image"
                loading="lazy"
              >
              
              <div class="modern-cart-item__details">
                <h3 class="modern-cart-item__title">{{ item.product.title | escape }}</h3>
                
                {%- if item.product.has_only_default_variant == false -%}
                  <p class="modern-cart-item__variant">
                    {%- for option in item.options_with_values -%}
                      {{ option.value }}{% unless forloop.last %} / {% endunless %}
                    {%- endfor -%}
                  </p>
                {%- endif -%}
                
                <div class="modern-cart-item__quantity">
                  <div class="modern-cart-item__quantity-controls">
                    <button 
                      type="button" 
                      class="modern-cart-item__quantity-btn"
                      onclick="updateQuantity({{ item.index | plus: 1 }}, {{ item.quantity | minus: 1 }})"
                    >-</button>
                    <input 
                      type="number" 
                      class="modern-cart-item__quantity-input"
                      value="{{ item.quantity }}"
                      min="1"
                      data-index="{{ item.index | plus: 1 }}"
                      onchange="updateQuantity({{ item.index | plus: 1 }}, this.value)"
                    >
                    <button 
                      type="button" 
                      class="modern-cart-item__quantity-btn"
                      onclick="updateQuantity({{ item.index | plus: 1 }}, {{ item.quantity | plus: 1 }})"
                    >+</button>
                  </div>
                  <button 
                    type="button" 
                    class="modern-cart-item__remove"
                    onclick="removeItem({{ item.index | plus: 1 }})"
                  >Remove</button>
                </div>
              </div>
              
              <div class="modern-cart-item__price">
                {%- if item.original_price != item.final_price -%}
                  <div class="modern-cart-item__price-current">{{ item.final_price | money }}</div>
                  <div class="modern-cart-item__price-original">{{ item.original_price | money }}</div>
                {%- else -%}
                  <div class="modern-cart-item__price-current">{{ item.original_price | money }}</div>
                {%- endif -%}
              </div>
            </div>
          {%- endfor -%}
        </form>
      </div>

      <!-- Gift Bag Option -->
      <div class="modern-cart__gift-bag">
        <img 
          src="{{ 'icon-gift.svg' | asset_url }}" 
          alt="Gift Bag"
          class="modern-cart__gift-bag-image"
        >
        <span class="modern-cart__gift-bag-text">ADD GIFT BAG FOR ₹470</span>
        <input type="checkbox" class="modern-cart__gift-bag-checkbox">
      </div>

      <!-- Order Summary -->
      <div class="modern-cart__summary">
        <div class="modern-cart__summary-row">
          <span>Subtotal</span>
          <span>{{ cart.total_price | money }}</span>
        </div>
        <div class="modern-cart__summary-row modern-cart__summary-total">
          <span>Total</span>
          <span>{{ cart.total_price | money }}</span>
        </div>
        
        <button type="submit" form="cart" class="modern-cart__checkout-btn">
          CHECKOUT NOW SAFELY
        </button>
        
        <div class="modern-cart__view-bag">View Bag</div>
      </div>

      <!-- Frequently Bought Together -->
      <div class="modern-cart__recommendations">
        <h2 class="modern-cart__recommendations-title">FREQUENTLY BOUGHT TOGETHER</h2>
        <div class="modern-cart__recommendations-grid">
          <div class="modern-cart__recommendation-item">
            <img src="{{ 'product-placeholder.jpg' | asset_url }}" alt="Product" class="modern-cart__recommendation-image">
            <h3 class="modern-cart__recommendation-title">SKIMS ULTIMATE teardrop push-up bra</h3>
            <div class="modern-cart__recommendation-price">₹45,958</div>
            <button class="modern-cart__recommendation-heart">♥</button>
          </div>
          <div class="modern-cart__recommendation-item">
            <img src="{{ 'product-placeholder.jpg' | asset_url }}" alt="Product" class="modern-cart__recommendation-image">
            <h3 class="modern-cart__recommendation-title">FITS EVERYBODY push-up bra</h3>
            <div class="modern-cart__recommendation-price">₹45,400</div>
            <button class="modern-cart__recommendation-heart">♥</button>
          </div>
        </div>
      </div>

      <!-- Looking for More -->
      <div class="modern-cart__recommendations">
        <h2 class="modern-cart__recommendations-title">LOOKING FOR MORE ?</h2>
        <div class="modern-cart__recommendations-grid">
          <div class="modern-cart__recommendation-item">
            <img src="{{ 'product-placeholder.jpg' | asset_url }}" alt="Product" class="modern-cart__recommendation-image">
            <h3 class="modern-cart__recommendation-title">SKIMS ULTIMATE teardrop push-up bra</h3>
            <div class="modern-cart__recommendation-price">₹45,958</div>
            <button class="modern-cart__recommendation-heart">♥</button>
          </div>
          <div class="modern-cart__recommendation-item">
            <img src="{{ 'product-placeholder.jpg' | asset_url }}" alt="Product" class="modern-cart__recommendation-image">
            <h3 class="modern-cart__recommendation-title">FITS EVERYBODY push-up bra</h3>
            <div class="modern-cart__recommendation-price">₹45,400</div>
            <button class="modern-cart__recommendation-heart">♥</button>
          </div>
        </div>
      </div>

      <!-- Policies -->
      <div class="modern-cart__policies">
        <div class="modern-cart__policy-item">
          <span class="modern-cart__policy-text">Our Policies</span>
          <span class="modern-cart__policy-arrow">›</span>
        </div>
        <div class="modern-cart__policy-item">
          <span class="modern-cart__policy-text">Terms & Conditions</span>
          <span class="modern-cart__policy-arrow">›</span>
        </div>
        <div class="modern-cart__policy-item">
          <span class="modern-cart__policy-text">Shipping & Processing</span>
          <span class="modern-cart__policy-arrow">›</span>
        </div>
        <div class="modern-cart__policy-item">
          <span class="modern-cart__policy-text">Returns</span>
          <span class="modern-cart__policy-arrow">›</span>
        </div>
      </div>
    {%- endif -%}
  </div>
</cart-items>

<script>
function updateQuantity(index, quantity) {
  if (quantity < 1) return;
  
  const form = document.getElementById('cart');
  const input = form.querySelector(`input[data-index="${index}"]`);
  input.value = quantity;
  form.submit();
}

function removeItem(index) {
  const form = document.getElementById('cart');
  const input = form.querySelector(`input[data-index="${index}"]`);
  input.value = 0;
  form.submit();
}
</script>

{% schema %}
{
  "name": "Modern Cart",
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 36
    }
  ]
}
{% endschema %}
